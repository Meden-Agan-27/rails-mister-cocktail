<h1>My cocktails</h1>

<% @cocktails.each do |c| %>
<ul>
  <li>
    <%= link_to c.name, cocktail_path(c) %>
    <%= cl_image_tag(c.picture.key, width: 400, height: 300, crop: :fill) %>
  </li>
</ul>
<% end %>
<%= link_to "create a new cocktail", new_cocktail_path %>
